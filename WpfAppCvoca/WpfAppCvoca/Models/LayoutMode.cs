namespace WpfAppCvoca.Models
{
    /// <summary>
    /// Step 0 (Show All)의 레이아웃 모드
    /// </summary>
    public enum LayoutMode
    {
        WordOnly,              // Word Only
        DefinitionOnly,        // Definition Only
        ExampleOnly,           // Example Only
        WordDefinition,        // Word + Definition
        DefinitionExample,     // Definition + Example
        WordDefinitionExample  // Word + Definition + Example
    }
}

